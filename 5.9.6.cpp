#include<iostream>


int main() {
	char ch[10][10];
	char c[5];
	int i = 0;
	/*
	while (1) {
		scanf_s("%s", ch[i]);
		if (ch[i] == "Quit") break;
		else printf("%s", *ch[i++]);
	}
	*/
	scanf_s("%s", c);
	return 0;
}